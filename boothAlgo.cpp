#include<iostream> 
#include<bits/stdc++.h>
using namespace std;
int algo(int m, int q) {
    int a = 0, q1 = 0, q0;
    int count = q;
    while(count > 0) {
        
        if(q & q1 == false) {
            
        }

        count--;
    }
    
}
int main() {

    int m, q;
    cout<<"Enter the multiplicand and multiplier: "<<endl;
    cin>>m>>q;
    int ans = algo(m, q);

    return 0;
}